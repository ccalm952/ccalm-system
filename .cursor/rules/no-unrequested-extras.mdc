---
description: 不要擅自添加用户未要求的功能、属性或样式
alwaysApply: true
---

# 只实现用户明确要求的内容

实现或修改代码时，**不要主动添加用户未要求的东西**。

## 禁止擅自添加（除非用户明确要求）

- 无障碍属性：`aria-label`、`aria-describedby`、`aria-hidden`、`role` 等
- Label 与 Input 的 `htmlFor` / `id` 配对（仅为点击标签聚焦或读屏）
- 额外注释块、文档、防御性兜底、未要求的 `ref` / `memo` / `useCallback`
- 未要求的 `className`、布局、动画、错误处理、日志

## 默认写法

```tsx
// ❌ 用户未要求时不要写
<Label htmlFor="total-income">总收入</Label>
<Input id="total-income" ... />
<Button aria-label="删除"><X /></Button>

// ✅ 用户未要求时
<Label>总收入</Label>
<Input ... />
<Button type="button" onClick={onRemove}><X /></Button>
```

## 例外

- shadcn/ui **registry 官方文件**里自带的 `aria-*`：保留官方默认，覆盖官方时不要额外再加一层
- 用户**明确**要求无障碍、表单关联、某类样式或行为时，按用户说明实现
